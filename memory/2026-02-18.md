# 2026-02-18 — DG Solver Project State

## Project
DG solver library at `/home/philipnickel/.openclaw/workspace-numerical`, GitHub: `Eddyassistant/dg-solver`

## What's Done
- 1D modal DG (Legendre, Rusanov/Godunov/EO, SSP-RK2/3/RK4, TVB limiter) — optimal O(h^{p+1}) P0–P4
- 2D nodal DG on triangles (PKD basis, fused Numba kernel, strong-form) — verified h/p convergence (P1→P5)
- 56 tests passing
- GitHub issues #1–#5, #7–#9 open; #6 closed
- Branches merged to main: `feature/system-dg`
- `feature/ins-cavity` branch (not pushed): AC INS solver, SIPG viscous, cavity mesh/BCs, regularized lid
- Cavity at Re=100: stable at t≈15, residuals ~0.008 — effectively steady state

## What's Next
1. **Optimize `_extract_face_values`** in `solver2d_system.py` (~line 222) — Python loop over face points is bottleneck (0.18ms vs 0.03ms for Numba RHS kernel). Replace with Numba jit or vectorized NumPy.
2. **Run cavity to full steady state** (t=20-30) — needs longer timeout or checkpoint/restart
3. **Generate Ghia comparison plots** (centerline u(y) at x=0.5, v(x) at y=0.5)
4. **Commit/push `feature/ins-cavity`**, merge to main
5. Remaining issues: #1 Warp&Blend, #2 Euler, #4 limiters, #8 2D tests, #9 CI

## Key Technical Decisions
- AC method: q=[u,v,p], β=1.0 for Re=100
- SIPG viscous (not BR2): penalty σ_IP = C*(p+1)*(p+2)*ν/2, C_ip=4.0
- Regularized lid: 16x²(1-x)² profile
- Anti-symmetric gradient reflection at walls for viscous BCs
- CFL=0.008 for stability at nx=12, P2
- Equidistant nodes (Warp&Blend had bugs at p≥3)

## User
PhilipNickel on Discord, #numerical channel. Wants efficient code, no slow Python loops in hot paths.
